import Connect from "../../components/Connect";

export const metadata = { title: "Connect — SANGAM" };

export default function ConnectPage() {
  return (
    <main>
      <Connect />
    </main>
  );
}
